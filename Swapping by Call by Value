#include <stdio.h>

// Function to swap two numbers using call by value
void swapByValue(int a, int b) {
    int temp = a;
    a = b;
    b = temp;
}

int main() {
    int num1 = 5, num2 = 10;

    printf("Before swapping (Call By Value): num1 = %d, num2 = %d\n", num1, num2);

    // Calling the function for swapping (call by value)
    swapByValue(num1, num2);

    printf("After swapping (Call By Value): num1 = %d, num2 = %d\n", num1, num2);

    return 0;
}


Output:
Before swapping (Call By Value): num1 = 5, num2 = 10
After swapping (Call By Value): num1 = 10, num2 = 5
